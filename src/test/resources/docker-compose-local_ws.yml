services:
  afterimage-db:
    image: "redis:8.0.0"
    hostname: localhost
    ports:
      - "6380:6379"
    healthcheck:
      test: [ "CMD", "redis-cli" ,"ping", "afterimage-db" ]
      retries: 3
      interval: 1s
      timeout: 2s
    networks:
      docker-network:
volumes:
  afterimage-db:
    driver: local
networks:
  docker-network:
    driver: bridge
